<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Parcial1" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="buttonStyle">@style/CustomButtonStyle</item>
    </style>
    <style name="CustomButtonStyle" parent="Widget.Material3.Button">
        <item name="android:backgroundTint">#ff0000</item> <!-- Color de fondo personalizado -->
        <item name="android:textColor">#FFFFFF</item> <!-- Color del texto -->
    </style>

    <style name="Theme.Parcial1" parent="Base.Theme.Parcial1" />
</resources>